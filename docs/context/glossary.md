# Glossary

このファイルは、プロジェクト内で使う用語を整理します。

## 用語一覧

| 用語 | 意味 | 備考 |
|---|---|---|
| プロジェクト | このテンプレートを使って作成する学習対象の開発プロジェクト | 題材に応じて意味を具体化する |
| MVP | Minimum Viable Product | 最初に作る最小構成 |
| 要件定義 | 何を作るか、誰のために作るかを整理する工程 | 実装前に行う |
| 基本設計 | システム全体の構成や画面・機能の概要を整理する工程 | 外部設計とも呼ばれる |
| 詳細設計 | 実装に近い粒度で処理やデータ構造を整理する工程 | 内部設計とも呼ばれる |
| レビュー | 作成物の問題点や改善点を確認すること | 設計書・コードの両方に対して行う |
| 永続コンテキスト | AIが次回以降も参照するための文脈情報 | `docs/context/` や `docs/logs/` に保存 |
| クライアントモード | ユーザーがクライアント役、AIがPM役として要件を整理するモード | `【クライアント】` で開始 |
| 終了報告 | 作業終了時にログと次回タスクを整理する運用 | `終了報告` で実行 |
| 認証 | 利用者が誰であるかを確認すること | ログイン処理が代表例 |
| 認可 | 認証済みユーザーが、その操作を実行してよいか確認すること | ロール、参加プロジェクト、所有者などで判定する |
| システムロール | システム全体に対する権限区分 | 本プロジェクトでは `SYSTEM_ADMIN` と `USER` |
| プロジェクトロール | プロジェクト内における権限区分 | 本プロジェクトでは `PROJECT_ADMIN`、`MEMBER`、`VIEWER` |
| API | フロントエンドとバックエンドがデータをやり取りするための窓口 | REST APIとして設計している |
| REST API | URLとHTTPメソッドを使ってリソースを操作するAPI方式 | 本プロジェクトのAPI方式 |
| HTTPメソッド | APIに対して何をしたいかを表す操作種別 | `GET`、`POST`、`PUT`、`PATCH`、`DELETE` など |
| GET | データを取得するHTTPメソッド | 一覧取得、詳細取得など |
| POST | 新しいデータ作成や処理実行に使うHTTPメソッド | ログイン、作成、申請など |
| PUT | 既存データ全体の更新に使うHTTPメソッド | タスク更新、プロジェクト更新など |
| PATCH | 既存データの一部更新に使うHTTPメソッド | タスクStatus変更など |
| DELETE | データ削除に使うHTTPメソッド | 本プロジェクトでは主に論理削除 |
| APIパス | APIを呼び出すためのURL | 例: `/api/projects/{projectId}/tasks` |
| リクエスト | フロントエンドからバックエンドへ送る要求 | メソッド、パス、ヘッダー、ボディなどで構成される |
| レスポンス | バックエンドからフロントエンドへ返す結果 | ステータスコード、ヘッダー、ボディなどで構成される |
| JSON | データをキーと値の組み合わせで表す形式 | APIリクエスト、レスポンスで使用する |
| JSONを書く | APIへ送る値やAPIから返る値をJSON形式で記述すること | 設計書では項目構造を明確にする目的で書く |
| multipart/form-data | ファイルと通常の入力値を同時に送るためのリクエスト形式 | 添付ファイルアップロードで使用する |
| HTTPステータス | API処理結果を表す番号 | `200`、`400`、`401`、`403`、`404`、`500` など |
| 401 Unauthorized | 認証されていないことを表すHTTPステータス | 未ログイン時など |
| 403 Forbidden | 認証済みだが権限がないことを表すHTTPステータス | 権限不足、CSRF不正など |
| ページング | 一覧データを一定件数ごとに分けて取得・表示する方式 | プロジェクト一覧などで使用する |
| ステートレス | サーバーが前回のリクエスト状態を保持しない考え方 | JWT方式の説明で登場 |
| セッション | ログイン中の利用者状態をサーバー側で管理する仕組み | 本プロジェクトの認証方式 |
| セッション方式 | ログイン状態をサーバー側のセッションで管理する認証方式 | CookieのセッションIDと紐づける |
| JWT方式 | トークン自体にユーザー情報や有効期限を持たせる認証方式 | 初期MVPでは採用しない |
| Cookie | ブラウザに保存され、リクエスト時に送信される小さなデータ | セッションID管理に使う |
| JSESSIONID | Spring Bootで使われる標準的なセッションCookie名 | 本プロジェクトの基本方針 |
| CSRF | ログイン済みユーザーに意図しないリクエストを送らせる攻撃 | セッション方式では対策が必要 |
| CSRFトークン | 正規画面からのリクエストであることを確認するための値 | 状態変更APIで送信する |
| CSRFトークン取得API | フロントエンドがCSRFトークンを取得するAPI | 本プロジェクトでは `/api/csrf-token` |
| Spring Security | Spring Bootで認証・認可・CSRF対策などを扱う仕組み | セッション方式とCSRF対策で使用する |
| ビルドツール | ソースコードのビルド、テスト、依存関係管理を行う道具 | 本プロジェクトではMaven |
| Maven | Javaプロジェクトのビルドツール | Java 25 / Spring Boot構成で使用する |
| DBアクセス | アプリケーションからDBへ読み書きすること | Repository層が担当する |
| Spring Data JPA | JavaのEntityとRepositoryを使ってDBアクセスする仕組み | 本プロジェクトのDBアクセス方式 |
| DBマイグレーション | DB構造の変更を履歴ファイルとして管理すること | Flywayで管理する |
| 手動SQL | 開発者が直接SQLを実行してDBを変更する方法 | 履歴管理が弱く、初期MVPでは基本方針にしない |
| Flyway | SQLファイルでDBマイグレーションを管理するツール | 本プロジェクトで採用 |
| Liquibase | XML、YAML、JSON、SQLなどでDB変更を管理できるマイグレーションツール | Flywayより高機能だが学習コストが高め |
| Docker | アプリやミドルウェアをコンテナとして動かすための仕組み | 開発環境でMySQLとMailpitを動かす |
| Docker Compose | 複数のDockerコンテナをまとめて起動・停止・設定する仕組み | 本プロジェクトではMySQLとMailpitを起動する |
| コンテナ | アプリやミドルウェアを隔離された環境で動かす単位 | MySQLコンテナ、Mailpitコンテナなど |
| volume | Dockerコンテナのデータを永続化するための保存領域 | MySQLデータ保持に使用する |
| Mailpit | 開発中に送信メールをブラウザで確認するためのツール | 本プロジェクトで採用 |
| MailHog | Mailpitと同様に開発用メール確認を行うツール | 今回はMailpitを採用 |
| Service | 業務ルール、トランザクション、認可判定を担当するバックエンド層 | Controllerから呼び出される |
| Controller | HTTPリクエストを受け取り、Serviceを呼び出すバックエンド層 | 業務判断は持たせない |
| Repository | DBアクセスを担当するバックエンド層 | Spring Data JPAで実装する |
| Entity | DBテーブルに対応するJava側のデータ構造 | `User`、`Project`、`Task` など |
| DTO | APIリクエスト・レスポンス用のデータ構造 | Entityをそのまま外部公開しないために使う |
| トランザクション | 複数のDB操作をひとまとまりとして成功・失敗させる仕組み | Service層で制御する |
| 論理削除 | データを物理的に消さず、削除日時などで削除扱いにする方法 | 本プロジェクトの削除方針 |
| 物理削除 | DBからレコードを実際に削除する方法 | 初期MVPでは主要データに対して基本採用しない |
| ロール | 操作権限を分類するための役割 | システムロールとプロジェクトロールがある |
| 権限表示制御 | 権限に応じてボタンやメニューの表示・非表示を切り替えること | 最終判定はバックエンドで行う |
| Next.js | Reactをベースにしたフロントエンドフレームワーク | 本プロジェクトのフロントエンド |
| React | UIをコンポーネントで構築するJavaScriptライブラリ | Next.jsの基盤 |
| TypeScript | JavaScriptに型を加えた言語 | フロントエンドで使用する |
| Tailwind CSS | ユーティリティクラスで画面スタイルを作るCSSフレームワーク | UIの基本方針 |
| shadcn/ui | Tailwind CSSと組み合わせて使うUIコンポーネント群 | 必要に応じて採用 |
| TanStack Query | APIから取得したサーバー状態を管理するライブラリ | プロジェクト一覧、タスク一覧、メンバー一覧などに使う |
| サーバー状態 | APIから取得し、バックエンド側を正とするデータ | TanStack Queryで管理する方針 |
| 画面状態 | モーダル開閉や入力中フォームなど画面内で完結する状態 | React標準で管理する |
| useState | Reactで単純な画面状態を管理するための仕組み | モーダル開閉、入力欄など |
| useReducer | 複数の状態を操作単位でまとめて管理するReactの仕組み | 複雑なフォームや連動する状態に向く |
| キャッシュ | 取得済みデータを一時的に保持して再利用すること | TanStack Queryが扱いやすくする |
| 再取得 | 更新後などにAPIから最新データを取り直すこと | タスク作成後のカンバン再取得など |
| ローディング状態 | API取得中や送信中であることを表す状態 | 二重送信防止や待機表示に使う |
| エラー状態 | API失敗や入力不正などを画面に表示するための状態 | 画面または部品単位で管理する |
| モーダル | 現在の画面上に重ねて表示する入力・確認用UI | タスク作成・編集で使用する |
| ドラッグアンドドロップ | 要素をつかんで移動先へ置く操作 | カンバンのStatus変更で使用する |
| ワイヤーフレーム | 画面の配置や情報構造を簡易的に表した設計図 | 色や装飾よりも、画面構成、導線、表示項目を確認するために使う |
